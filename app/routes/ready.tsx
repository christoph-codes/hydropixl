import { Heading, Text } from "@radix-ui/themes";
import PageLayout from "~/templates/PageLayout";

const Ready = () => {
	return (
		<PageLayout>
			<Heading>Request Consultation</Heading>
			<Text>Long paragraph for the page content.</Text>
		</PageLayout>
	);
};

export default Ready;
